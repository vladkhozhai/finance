/**
 * Budget Card Component
 *
 * Displays a budget with visual progress indicator
 * Shows:
 * - Budget name (category or tag)
 * - Spent amount vs limit
 * - Progress bar with color indicators (green/red for over budget)
 * - Overspending warning if applicable
 */

export function BudgetCard() {
  return (
    <div>{/* Budget card with progress bar will be implemented here */}</div>
  );
}
