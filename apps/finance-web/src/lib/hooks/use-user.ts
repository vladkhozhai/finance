/**
 * useUser Hook
 *
 * Custom hook for accessing current authenticated user
 * Returns user object and loading state
 *
 * @client hook - uses Supabase client
 */

"use client";

export function useUser() {
  // Hook implementation will be added by Backend Developer
  return {
    user: null,
    loading: true,
  };
}
