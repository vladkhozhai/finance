{
  "name": "@platform/root",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format",
    "test": "turbo test",
    "validate-migrations": "tsx scripts/validate-migrations.ts"
  },
  "devDependencies": {
    "turbo": "^2.7.2",
    "tsx": "^4.21.0"
  }
}
